let todos = [];

window.onload = () => {
    // fetch all data from backed (url: /get)
    // add all the data to todos
    // then create li on myUL looping the data from todos. In li also append a checkbox input and set its defaultValue to todo[i].status
}

/*
Add.onclick = () => {
    fetch(/add, {method: 'POST', // create body}).then( // append the response to todo)
}
*/

